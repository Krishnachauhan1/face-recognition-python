[project]
name = "face-recognition-python"
version = "1.0.0"
description = "HRMS face embedding API (FastAPI + InsightFace)"
requires-python = ">=3.10"

[tool.vercel]
entrypoint = "face_service:app"

[tool.vercel.scripts]
build = "python preload_model.py"
